[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = .
addopts = -ra -m "not real_llm" --ignore=tests/test_pdf_chunking.py
asyncio_mode = auto
markers =
    real_llm: tests that call the configured real LLM provider and may consume API quota
